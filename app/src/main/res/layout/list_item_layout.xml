<!-- list_item_layout.xml -->
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- 按钮容器 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end">

        <!-- 第一个按钮 -->
        <ImageButton
            android:id="@+id/editbutton"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/edit_modify_icon"/>

        <!-- 第二个按钮 -->
        <ImageButton
            android:id="@+id/deletebutton"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/delete_trash_icon"/>
    </LinearLayout>

    <!-- 标题 -->
    <TextView
        android:id="@+id/item_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="List Item Title"
        android:textSize="18sp"
        android:textColor="@android:color/black"
        android:layout_marginBottom="4dp" />

    <!-- 描述文本 -->
    <TextView
        android:id="@+id/item_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:layout_below="@id/item_title"
        android:text="Description of the list item goes here. You can customize it as needed."
        android:textColor="@android:color/darker_gray"
        android:textSize="14sp" />

    <!-- 日期或其他信息 -->
    <TextView
        android:id="@+id/item_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/item_description"
        android:text="Date or additional info"
        android:textSize="12sp"
        android:textColor="@color/grey" />

</RelativeLayout>
